import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_vehiculo_front',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
